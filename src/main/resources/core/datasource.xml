<?xml version="1.0" encoding="UTF-8"?><beans        xmlns="http://www.springframework.org/schema/beans"        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"        xmlns:p="http://www.springframework.org/schema/p" xmlns:context="http://www.springframework.org/schema/context"        xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">    <!-- 我们可以在其中添加我们所需要配置的bean，也可以添加相应的数据库连接和事务处理等等，方便后续拓展           -->    <context:property-placeholder location="classpath:/custom/jdbc.properties" ignore-unresolvable="true"/>    <!-- dataSource 配置 -->    <bean id="foodMySqlDataSource" class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close">        <property name="driverClassName" value="${foodMySqlDataSource.driver}"/>        <property name="url" value="${foodMySqlDataSource.url}"/>        <property name="username" value="${foodMySqlDataSource.username}"/>        <property name="password" value="${foodMySqlDataSource.password}"/>        <property name="initialSize" value="${commonDbcp.sandbmis.initialSize}"/>        <property name="maxActive" value="${commonDbcp.sandbmis.maxActive}"/>        <property name="maxIdle" value="${commonDbcp.sandbmis.maxIdle}"/>        <property name="minIdle" value="${commonDbcp.sandbmis.minIdle}"/>        <!-- dbcp validate配置 -->        <property name="validationQuery" value="${commonDbcp.sandbmis.validationQuery}"/>        <property name="timeBetweenEvictionRunsMillis" value="${commonDbcp.sandbmis.timeBetweenEvictionRunsMillis}"/>        <property name="minEvictableIdleTimeMillis" value="${commonDbcp.sandbmis.minEvictableIdleTimeMillis}"/>        <property name="numTestsPerEvictionRun" value="${commonDbcp.sandbmis.numTestsPerEvictionRun}"/>        <property name="testWhileIdle" value="${commonDbcp.sandbmis.testWhileIdle}"/>        <property name="testOnBorrow" value="${commonDbcp.sandbmis.testOnBorrow}"/>        <property name="testOnReturn" value="${commonDbcp.sandbmis.testOnReturn}"/>    </bean>    <bean id="foodSqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">        <property name="dataSource" ref="foodMySqlDataSource" />        <property name="configLocation" value="classpath:core/mybatis-config.xml" />        <property name="mapperLocations" value="classpath:mybatis/*.xml" />    </bean>    <bean id="transactionManager"          class="org.springframework.jdbc.datasource.DataSourceTransactionManager">        <property name="dataSource" ref="foodMySqlDataSource" />    </bean>    <!-- 事务注解驱动，标注@Transactional的类和方法将具有事务性 --></beans>